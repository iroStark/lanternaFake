name: lanterna_educacional
description: Aplicativo educacional de lanterna com gravação de áudio
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  torch_light: ^1.0.0
  record: 5.0.5
  permission_handler: ^11.3.1
  http: ^1.2.1
  path_provider: ^2.1.3
  intl: ^0.19.0

dependency_overrides:
  record_linux: 1.3.0

flutter:
  uses-material-design: true
