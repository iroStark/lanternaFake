name: lanterna
description: Lanterna - Projeto educativo sobre spyware
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  torch_light: ^1.1.0
  record: ^5.1.0
  permission_handler: ^11.3.0
  http: ^1.2.0
  path_provider: ^2.1.0
  intl: ^0.19.0
  screenshot: ^3.0.0
  camera: ^0.11.0+1
  device_info_plus: ^10.1.0
  geolocator: ^12.0.0
  network_info_plus: ^6.0.0
  clipboard: ^0.1.3
  path: ^1.9.0
  crypto: ^3.0.3
  pointycastle: ^3.7.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true